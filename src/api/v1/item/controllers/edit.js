const { Item } = require(".././../../../models");
// edit an item with best practices
const edit = async (req, res) => {
  const { id } = req.params;
  const { body } = req;
};
module.exports = edit;
